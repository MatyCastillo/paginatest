<!doctype html>
<html lang="es">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta
			name="description"
			content="Informática Superior crea sitios web, ecommerce y soluciones de software a medida para negocios que quieren crecer digitalmente."
		/>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Informática Superior | Desarrollo Web y Software</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--bg: #f4f7fb;
		--bg-soft: #eef4ff;
		--panel: #ffffff;
		--panel-strong: #edf3ff;
		--primary: #1e4ed8;
		--primary-strong: #163bb3;
		--secondary: #7c3aed;
		--text: #111827;
		--muted: #4b5563;
		--line: rgba(17, 24, 39, 0.08);
		--card: #ffffff;
		--shadow: 0 24px 55px rgba(15, 23, 42, 0.09);
	}

	* {
		box-sizing: border-box;
	}

	html {
		scroll-behavior: smooth;
	}

	body {
		margin: 0;
		font-family: Inter, 'Segoe UI', sans-serif;
		background: linear-gradient(180deg, #f9fbff 0%, #eef4ff 100%);
		color: var(--text);
		line-height: 1.6;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	img {
		max-width: 100%;
		display: block;
	}
</style>
